<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.ittepic.miniu1_anacarolinamondragonrangel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".IniciarSesion"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Seleccionmascota"
            android:label="PAW PROFILE" />
        <activity
            android:name=".REGISTRO"
            android:label="REGISTRARSE" />
        <activity
            android:name=".SiguienteRegistro"
            android:label="REGISTRO MASCOTA" />
        <activity android:name=".Alimento" />
        <activity
            android:name=".Calendario"
            android:label="CALENDARIO" />
        <activity android:name=".Acercade" android:label="ACERCA DE"></activity>
    </application>

</manifest>